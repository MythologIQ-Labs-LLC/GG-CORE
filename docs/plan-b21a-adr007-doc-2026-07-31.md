# Plan: B-21a — Canonical ADR-007 Doc + Backlog Reconciliation

**change_class**: hotfix

**doc_tier**: standard

**terms_introduced**: []

**boundaries**:
- limitations:
  - Brings the canonical `ADR-007-TIERSYNERGY-ADAPTIVE-SPECULATIVE-DECODING.md` onto `main` (from
    the unmerged PR #59 branch), reconciled to the built reality, so the 5 dangling FEATURE_INDEX
    citations resolve; and reconciles the BACKLOG so B-21's #61–#68 read as done and the remainder
    (B-21b-1/c/b-2/d/e) is explicit. Documentation/governance only.
- non_goals:
  - No Rust/behavior change; no consolidation code, no wiring (those are B-21b-1/c).
  - No change to the sealed speculative implementations.
- exclusions:
  - No CI/workflow change.

## Open Questions

None. The PR #59 doc exists and is retrievable (`origin/docs/adr-007-tiersynergy-dspark:docs/
architecture/ADR-007-TIERSYNERGY-ADAPTIVE-SPECULATIVE-DECODING.md`, 566 lines, Status: Proposed);
the built reality (#61–#68 sealed #87–#94; built-but-dormant; triple redundancy) is established by
the B-21 scoping brief (ledger #180).

## Design Rationale (Simple Made Easy)

The design of record already exists on the unmerged PR #59 branch; five FEATURE_INDEX rows cite it.
Rather than author a competing doc, adopt the existing ADR onto `main` and reconcile the two things
that drifted since it was written: its **Status** (the design was implemented + sealed, so it is no
longer merely "Proposed") and a short **Implementation Status & Consolidation** addendum recording
the built-but-dormant reality, the triple-redundancy (v1/v2/adaptive), and the confirmed retirement
sequence (v1→v2→single-adaptive across B-21b-1/c/b-2). This resolves the doc-drift and makes the
canonical decision auditable without inventing content.

## Phase 1: Adopt + reconcile the ADR doc

### Affected Files

- `docs/architecture/ADR-007-TIERSYNERGY-ADAPTIVE-SPECULATIVE-DECODING.md` (NEW on `main`) —
  materialized from the PR #59 branch (`git show origin/docs/adr-007-tiersynergy-dspark:<path>`),
  then reconciled:
  - **Status** line: `Proposed` → `Accepted — implemented (dormant), see Implementation Status`.
  - Append an **## Implementation Status & Consolidation (2026-07-31, B-21)** section: the epic
    (#61–#68) is built + unit-tested + sealed (ledger #87–#94; features F-48–F-54) but **dormant**
    (no `Runtime::infer` wiring); three parallel impls exist (`speculative.rs` v1,
    `speculative_v2.rs`, `adaptive_speculative/`); **`adaptive_speculative/` is canonical**, v1 and
    v2 retire via B-21b-1 (delete v1, port GGUF adapter to v2) → B-21c (adaptive executor + GGUF
    backend + wire) → B-21b-2 (delete v2); a real e2e benchmark follows (B-21e). Cross-reference the
    scoping brief `docs/research-brief-b21-adr007-scoping-2026-07-31.md` and
    `ADR-007-CONSOLIDATION-AUDIT.md` (noting the latter predates `adaptive_speculative` and is
    superseded by this section).
- The body is adopted verbatim from PR #59 except the Status line + the appended section (it is the
  design the code was built to; no rewrite).

### Unit Tests

No unit test (documentation). Verification: the file exists at the cited path; the 5 FEATURE_INDEX
rows (F-48/49/50/51/53) that cite it now resolve; the appended section's factual claims (sealed
entries, dormancy, redundancy) match the scoping brief #180.

## Phase 2: Backlog reconciliation

### Affected Files

- `docs/BACKLOG.md` — re-scope the B-21 row: note #61–#68 are DONE (sealed #87–#94, F-48–F-54); the
  remainder is B-21a (this doc), B-21b-1 (retire v1), B-21c (adaptive executor + wire, L3), B-21b-2
  (retire v2), B-21d (loose ends: audit F2/F3 + missing `t1_..allowlist` test), B-21e (e2e
  benchmark). Reflects the operator-confirmed sequence.

### Unit Tests

No unit test. Verification: the B-21 row reads the reframed decomposition; no sub-item is lost.

## Feature Inventory Touches

Empty — justified. Adds the design doc the existing F-48–F-53 rows already cite (resolving dangling
references) + reconciles the backlog; introduces/modifies no user-touchable runtime feature.

## Definition of Done

### Deliverable: canonical ADR on main + reconciled backlog

- **D1**: `ADR-007-TIERSYNERGY-ADAPTIVE-SPECULATIVE-DECODING.md` exists on `main`, reconciled to the
  built-dormant reality; FEATURE_INDEX rows citing it resolve; the backlog reflects the confirmed
  B-21 decomposition.
- **D2**: NEW reconciled ADR doc; `BACKLOG.md` B-21 row re-scoped.
- **D3**: META_LEDGER entries (canonical markup) plan, audit, seal; governance-index registers the
  ADR doc.
- **D4**: `test -f docs/architecture/ADR-007-TIERSYNERGY-ADAPTIVE-SPECULATIVE-DECODING.md`; `grep -c`
  confirms the reconciled Status + Implementation-Status section; `grep -L` shows no FEATURE_INDEX
  citation of the ADR now dangles (the file exists).

## CI Commands

- `python -c "import pathlib; assert pathlib.Path('docs/architecture/ADR-007-TIERSYNERGY-ADAPTIVE-SPECULATIVE-DECODING.md').exists()"` — the ADR doc exists on the branch
- `qor-logic scripts feature_index_verify --repo-root .` — index still resolves (60/60)
